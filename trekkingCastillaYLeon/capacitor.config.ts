import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.trekkingcyl',
  appName: 'trekkingCastillaYLeon',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
